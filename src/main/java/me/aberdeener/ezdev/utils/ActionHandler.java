package me.aberdeener.ezdev.utils;

import org.bukkit.entity.Player;

public class ActionHandler {

    // We will be moving the switch(action){} here so that we dont have repeat code for each command, event, runnable
    public void handleAction(Player sender, String action, String target, String value) {
    }

}
